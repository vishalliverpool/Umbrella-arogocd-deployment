# Local-only — never push to git
eksctl/
bootstrap/

# Local overrides
values-local.yaml
*.local.yaml
.env

# Helm: pulled OCI subcharts — resolved at deploy time, never committed
sinec-app/charts/
sinec-app/*.tgz
