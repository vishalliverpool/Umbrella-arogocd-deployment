apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sinec-platform
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/vishalliverpool/Umbrella-arogocd-deployment.git
    targetRevision: main
    path: umbrella
  destination:
    server: https://kubernetes.default.svc
    namespace: sinec-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
