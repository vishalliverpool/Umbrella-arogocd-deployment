apiVersion: v2
name: orders
description: Helm chart for the orders microservice
type: application
# version (chart) and appVersion (image tag) are overwritten by the release pipeline from the git tag.
version: 0.1.0
appVersion: "1.0.0"
