apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: payments
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # Chart lives in THIS git repo. The sync-charts pipeline pulls the latest
    # chart from ECR and unpacks it into services/payments, then commits.
    repoURL: https://github.com/vishalliverpool/Umbrella-arogocd-deployment.git
    targetRevision: main
    path: services/payments
  destination:
    server: https://kubernetes.default.svc
    namespace: sinec-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
